{
  "name": "auth-terminal",
  "version": "1.0.0",
  "description": "A minimal terminal-style interface to interact with an authentication API",
  "scripts": {
    "dev": "node scripts/inject-env.js && tsc --watch",
    "build": "node scripts/inject-env.js && tsc",
    "serve": "python3 -m http.server 8000 --bind 127.0.0.1"
  },
  "keywords": [
    "terminal",
    "auth",
    "typescript",
    "vanilla"
  ],
  "author": "Gustavo Castanho",
  "license": "MIT",
  "devDependencies": {
    "dotenv": "^16.4.5",
    "typescript": "^5.7.2"
  }
}